name: Auto Trade Runner

on:
  schedule:
    # Morning report: 7:00 AM NPT (01:15 UTC), Mon-Fri
    - cron: '15 1 * * 1-5'
    # Pre-market report + data refresh: 10:30 AM NPT (04:45 UTC), Mon-Fri
    - cron: '45 4 * * 1-5'
    # Every hour during market hours: 11:00 AM–2:15 PM NPT (05:15–08:15 UTC), Mon-Fri
    - cron: '15 5,6,7,8 * * 1-5'
  workflow_dispatch:

jobs:
  trade:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # ── Cache pip packages ──────────────────────────────────────────────────
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ hashFiles('requirements.txt') }}
          restore-keys: pip-

      # ── Cache Playwright browser ────────────────────────────────────────────
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ hashFiles('requirements.txt') }}
          restore-keys: playwright-chromium-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browser
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: playwright install chromium --with-deps

      - name: Install Playwright system deps only (cached browser)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: |
          playwright install chromium
          playwright install-deps chromium

      - name: Run contract tests (fixtures)
        run: python -m pytest tests/ -v

      # ── Run bot ────────────────────────────────────────────────────────────
      - name: Run Trading Bot
        env:
          NAASA_USERNAME: ${{ secrets.NAASA_USERNAME }}
          NAASA_PASSWORD: ${{ secrets.NAASA_PASSWORD }}
          CHUKUL_USERNAME: ${{ secrets.CHUKUL_USERNAME }}
          CHUKUL_PASSWORD: ${{ secrets.CHUKUL_PASSWORD }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DRY_RUN: ${{ secrets.DRY_RUN }}
          MAX_DAILY_BUYS: ${{ secrets.MAX_DAILY_BUYS }}
          MAX_PORTFOLIO_STOCKS: ${{ secrets.MAX_PORTFOLIO_STOCKS }}
          DEFAULT_BUY_QTY: ${{ secrets.DEFAULT_BUY_QTY }}
          SWING_RSI_OVERSOLD: ${{ secrets.SWING_RSI_OVERSOLD }}
          SWING_RSI_OVERBOUGHT: ${{ secrets.SWING_RSI_OVERBOUGHT }}
          SWING_VOL_MULT: ${{ secrets.SWING_VOL_MULT }}
          SWING_VOL_PERIOD: ${{ secrets.SWING_VOL_PERIOD }}
          FETCH_MAX_WORKERS: ${{ secrets.FETCH_MAX_WORKERS }}
          RUN_ONCE: "True"
          POLL_INTERVAL: "3600"
          SLEEP_INTERVAL_CLOSED: "3600"
        run: python main.py

      # ── Persist daily order state (no CSVs — all data fetched fresh) ────────
      - name: Save order state
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}
          git add placed_orders_today.json portfolio_summary.json fortress_state.json swing_targets.json chukul_fundamental.csv chukul_data.csv 2>/dev/null || true
          git diff-index --cached --quiet HEAD || (git commit -m "Update order state" && git pull --rebase origin main && git push)
